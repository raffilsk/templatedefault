import type * as Stitches from '@stitches/react';

import * as S from './styles';

type {{pascalCase name}}ComponentProps = {
css?: Stitches.CSS;
};

export default function {{pascalCase name}}({ css, ...props }: {{pascalCase name}}ComponentProps) {
return (<S.Wrapper css={css} {...props}><span>{{pascalCase name}}</span></S.Wrapper>);
}
