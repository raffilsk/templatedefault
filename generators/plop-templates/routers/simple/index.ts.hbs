import type { NextApiRequest, NextApiResponse } from 'next';

export default function {{pascalCase name}}(req: NextApiRequest, res: NextApiResponse) {
const { name, age } = req.body;

res.status(200).json({ name, age });
}
